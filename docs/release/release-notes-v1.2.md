# 출시 노트 — versionName 1.2 / versionCode 4

Play Console "비공개 테스트 → 새 버전 만들기 → 출시 노트"에 그대로 붙여넣기.
언어: 한국어 – 대한민국 (`<ko-KR>`)

## 붙여넣기용 (498자 이내)

```
해몽이 훨씬 빨라졌습니다.

• 해몽 응답 속도 개선 — 첫 해몽까지 걸리던 시간을 크게 줄였습니다.
• 카드 읽기 개선 — 카드 한글 이름과 뜻을 그림 밖으로 옮겨 그림과 글자가 겹치지 않습니다.
• 꿈기록·통계 화면 여백 정리 — 제목이 상태바에 붙던 문제를 고쳤습니다.
• 홈 화면에서 길게 적은 꿈을 더 잘 보이게 다듬었습니다.
• 통계의 꿈 키워드에 그 상징의 의미를 함께 보여줍니다.
• Android 16 대응 — 최신 안드로이드에서도 정상 동작하도록 업데이트했습니다.

테스트 중 이상한 점이 보이면 편하게 알려주세요.
```

## 이 버전에 실제로 들어간 변경 (내부 기록)

| 커밋 | 내용 |
|---|---|
| `d43f7a0` | `/reading` 38초의 원인이던 Okt(JVM) 첫 호출을 기동 시 워밍업 + OpenAI 2회 병렬화 |
| `20d5d08` | 카드 한글 이름/뜻을 그림 밖(`CardLabel.tsx`)으로 분리 |
| `1ff69a9` | targetSdk/compileSdk 35→36(Android 16), buildTools 36.0.0, `enableOnBackInvokedCallback="false"` |
| `2acf5c0` | 홈 긴 꿈 강조 · 꿈기록 헤더 `paddingTop` 20→56 · 통계 키워드 의미 병기 |
| `b283b8b` | 위 `2acf5c0`가 담긴 `worktree-store-screenshots`를 main에 머지 (versionCode 4 빌드에 누락돼 있었음) |

## 검증 기록 (2026-08-26, 에뮬레이터 API 37 / Android 17)

- 설치본 targetSdk 36 · versionCode 4 확인
- 뒤로가기: 바텀시트 닫힘 / 스택 화면 복귀 / 루트에서 종료 — 전부 정상
- 인셋: 상단 상태바·하단 제스처바 모두 콘텐츠 잘림 없음
- e2e: 꿈 입력 → Render 백엔드 해몽 → 카드 → 꿈기록·통계 반영 성공
