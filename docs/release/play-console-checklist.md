# Play Console 업로드 치트시트

**현재 올릴 빌드: versionCode 4 / versionName 1.2 (2026-08-26 재빌드)**
`android/app/build/outputs/bundle/release/app-release.aab` (130MB)
- targetSdk 36(Android 16) — Play 2026-11-01 요구사항 충족. 이게 이번 업데이트의 목적이다.
- 업로드 키 서명·Render 백엔드 주소 구움·에뮬(API 37) 실기 검증 완료.
- 출시 노트 복붙용: `docs/release/release-notes-v1.2.md`
- ⚠️ versionCode 4는 아직 Play에 올라간 적 없음. 다시 고치면 5로 올릴 것.

인증이 풀린 뒤 이 순서대로 진행하면 됩니다. 값은 전부 복붙 가능하게 정리했습니다.
AAB: `android/app/build/outputs/bundle/release/app-release.aab` (프로덕션 백엔드 URL 구워짐·서명 완료 확인됨)

---

## 0. 제출 전 (권장) — 백엔드 콜드스타트 제거
- Render 대시보드에서 `monkey-backend` 플랜을 **starter**로 업그레이드(결제 발생).
  `render.yaml`은 이미 `starter`로 바꿔둠 → Blueprint 재동기화 또는 대시보드에서 플랜 변경.
- 이유: 클로즈드 테스트는 12명이 14일간 **실사용**해야 통과. free 플랜은 15분 유휴 후 슬립 →
  첫 요청 지연으로 테스터 이탈 위험.

## 1. 앱 만들기 — ✅ 이미 완료 (2026-07-17)
- 앱 이름(생성됨): `Monkey (夢Key; 꿈의 키워드) - 해몽, 운명`
- 패키지: **`com.mswon.monkey`** (영구 확정 → 로컬 build.gradle도 이걸로 맞춤, AAB 재빌드함)
- 상태: 임시(초안). 이 앱에 이어서 아래 단계 진행.

## 2. 스토어 등록정보 (붙여넣기)
- **앱 이름(30자)**: `Monkey - 꿈 해몽`
- **짧은 설명(80자)**:
  `전통 해몽으로 매일 밤 꿈을 기록하고, 아르누보 카드로 오늘의 상징을 만나보세요.`
- **자세한 설명**: `docs/release/store-listing.md`의 자세한 설명 블록 그대로 붙여넣기
- **그래픽 자산** (`docs/release/store-assets/`):
  ⚠️ 2026-08-26 정정 — 예전에 "Play Console은 `input[type=file]`을 DOM에 두지 않는다"고 적어뒀는데 **사실이 아니다.**
  출시 준비 페이지의 App Bundle 업로드 영역에 실제 file input이 존재한다(`find`로 확인됨).
  브라우저 자동화가 막히는 진짜 이유는 **업로드 도구의 10MB 한도**다. 따라서:
  - **AAB(130MB) → 사용자가 직접** 올려야 한다.
  - **스토어 그래픽(아이콘 463KB·피처그래픽·스크린샷 각 1MB 안팎) → 자동 업로드 가능성 있음.** 시도해 볼 것.
  - 앱 아이콘 512×512 → `icon-512.png` (463KB, 1MB 제한 충족)
  - 피처 그래픽 1024×500 → `feature-graphic-1024x500.png`
  - 폰 스크린샷 **5장**, 이 순서로 올리기 → `2-home` / `5-card` / `6-interpretation` / `3-diary-cards` / `4-stats` (전부 1080×2400)
    ⚠️ 2026-07-27에 **6장 전부 새 이미지로 교체**됐다(한글 꿈으로 재캡처 + 홍보문구 합성). 예전에 올린 게 있으면 지우고 다시 올릴 것.
    스플래시(`screenshot-1-splash`)는 뺐다.

## 진행 상황 (2026-07-26 갱신) — 앱 설정 10/11 완료
완료: 개인정보처리방침 / 로그인 세부정보(제한 없음) / 광고(없음) / 콘텐츠 등급(전 기관 최저 연령: ESRB E·PEGI 3·GRAC 전체이용가) /
타겟층(**만 18세 이상만**) / 데이터 보안 / 정부 앱(아니오) / 광고 ID(사용 안 함) / 앱 카테고리(라이프스타일)·연락처(xellos0304@gmail.com, 전화번호 미기재)
- 스토어 등록정보: 앱 이름·짧은 설명·자세한 설명 **저장 완료**. **그래픽 3종 업로드만 남음**(아래 4번).
- 데이터 보안 최종 신고 내용: 수집·공유 = `앱 활동 > 기타 사용자 제작 콘텐츠`(꿈 텍스트) **하나뿐**.
  전송 중 암호화 예 / 임시처리 아니요(OpenAI 최대 30일 보관) / 수집 필수 / 목적 앱 기능 / 삭제요청 창구 없음(90일 내 자동 삭제).

## 2-1. Play Console 실측 현황 (2026-08-26 직접 확인)

| 항목 | 값 |
|---|---|
| 개발자 계정 | MS Won · 계정 ID `7959811531958170355` |
| 앱 ID | `4973430615478884995` (`com.mswon.monkey`) |
| 앱 상태 | 비공개 테스트 · 프로덕션 비활성 |
| 트랙 | `비공개 테스트 - Alpha` · 트랙 ID `4699303582453749717` |
| **트랙에 현재 올라간 빌드** | **versionCode 1 (1.0), 타겟 SDK 35** — Play가 경고한 그 빌드 |
| 테스터 | **0명** (프로덕션 신청 요건: 12명 × 14일) |
| versionCode 2·3 | Play에 올라간 적 **없음** (로컬에서 폐기됨) |

**2026-08-26에 만들어 둔 초안**: 트랙 → 새 버전 만들기 → `releases/2/prepare`
- 출시명 `1.2 (4) - Android 16 대응`
- 출시 노트 한국어 입력 완료(`docs/release/release-notes-v1.2.md` 내용)
- **임시보관함에 저장까지 완료. AAB 업로드만 하면 `다음 → 미리보기 및 확인 → 출시 시작`.**

프로덕션 액세스 신청 진행률(대시보드): ✅ 비공개 테스트 버전 게시 / ⬜ 테스터 12명 / ⬜ 14일 실행

---

---

## 2-2. ⚠️ 업로드가 거부됐던 진짜 이유 — 서명 키 (2026-08-30 실측)

Play Console 임시 출시 draft에 남아 있던 실패 기록:

> `app-release.aab` — **"Android App Bundle이 잘못된 키로 서명되었습니다.
> 제대로 된 서명 키로 App Bundle에 서명한 다음 다시 시도해 보세요."**

즉 **업로드는 실제로 시도됐고 Play가 거부**한 것이다. 파일 크기나 파일 선택
문제가 아니었다. 이 앱은 **Play 앱 서명**을 쓰므로 AAB는 반드시 **업로드 키**로
서명돼 있어야 한다.

**Play가 기대하는 업로드 키 인증서** (앱 서명 → 업로드 키 인증서):

| | |
|---|---|
| SHA-256 | `AA:26:27:D0:A1:E4:73:55:DB:4A:CD:BF:16:0C:0A:13:DF:45:B1:A3:7A:9F:26:F9:2F:51:6C:4E:79:35:5C:A9` |
| SHA-1 | `EB:24:E1:22:A9:AF:DB:DB:3D:58:A5:59:EB:88:F9:90:89:2B:24:CE` |
| MD5 | `E3:0C:F4:C2:37:34:E7:F4:86:44:AD:D9:95:37:CF:92` |

**이 PC에서 빌드한 AAB는 위와 정확히 일치한다**(2026-08-30 확인). 따라서
거부된 파일은 다른 키로 서명된 것 — 가장 그럴듯한 원인은 **키스토어가 없는
다른 PC에서 빌드**한 경우다. `android/app/build.gradle`의 릴리스 서명 설정은
`keystore.properties`가 없으면 조용히 디버그 키로 폴백하므로, 빌드는 성공하지만
Play가 거부하는 AAB가 나온다(`docs/HANDOFF.md` 경로 주의).

**업로드 전 항상 확인할 것:**

```bash
cd <스크래치>
unzip -q -o "android/app/build/outputs/bundle/release/app-release.aab" -d aab
keytool -printcert -file aab/META-INF/*.RSA | grep SHA256
# => AA:26:27:D0:... 와 일치해야 한다. UPLOAD.RSA 가 아니라 CERT.RSA 면
#    디버그 키로 서명된 것이니 keystore.properties 부터 확인할 것.
```

정정: 이전 기록의 "막히는 건 업로드 도구 10MB 한도"는 **Claude 쪽 자동화 제약**일
뿐, Play가 거부한 이유가 아니다. 사용자가 브라우저에서 직접 올릴 때는 크기 제한이
걸리지 않는다(versionCode 1도 같은 크기로 올라갔다).

## 3. 앱 콘텐츠 (좌측 메뉴)
- **개인정보처리방침 URL**: `https://ms-won.github.io/Monkey/`
- **데이터 안전(Data safety)** — 아래 표대로 답변:

| 항목 | 답변 |
|---|---|
| 데이터 수집·공유 함? | **예** |
| 수집 유형 | **앱 활동 / 기타 사용자 콘텐츠**(꿈 텍스트) + **마이크(음성)** |
| 목적 | 앱 기능(해석 생성) |
| 제3자 공유 | **예 — OpenAI로 해석 생성 목적 전송** (광고/판매 아님) |
| 전송 중 암호화 | **예 (HTTPS)** |
| 기기 외 저장 | 꿈 기록은 **기기 로컬 저장**, 서버 미저장 (처리 후 미보관) |
| 계정/식별자 수집 | **없음** (앱에서 계정 생성·외부 로그인 모두 불가) |
| 이름 등 개인정보 | **미수집** — 온보딩 호칭은 기기 로컬 전용. ⚠️ 2026-07-26 이전 코드는 호칭을 `상담자 호칭:`으로 프롬프트에 실어 전송했음(`ResultScreen.tsx`). 제거 완료, AAB 재빌드함. 이 줄을 되돌리지 말 것 |
| 사용자 삭제 요청 | 앱 내 개별 삭제 + 앱 삭제 시 전체 삭제 |
| 음성 데이터 | 입력용, **녹음 미저장**(텍스트만 사용) |

- **콘텐츠 등급 설문**: 폭력/성적 콘텐츠 없음. 점술/오컬트 문항은 정직하게 답변.
- **타깃 연령**: 만 13세 이상(성인·일반). 아동 대상 아님.
- **광고**: 없음.

## 4. 클로즈드 테스트
- 테스트 → **비공개 테스트** 트랙 → **새 버전 만들기**
- `app-release.aab` 업로드 — ⚠️ **사용자가 직접**. Play Console은 네이티브 파일 선택창을 띄우므로
  브라우저 자동화로 파일을 넣을 수 없다(스토어 그래픽도 동일).
- 출시 노트: `docs/release/release-notes-v1.2.md`의 붙여넣기용 블록
- 테스터 이메일 목록 등록 (또는 Google 그룹)
- ⚠️ **신규 개인 계정: 최소 12명이 14일 연속 옵트인·실사용**해야 프로덕션 전환 가능.
  설치만 하고 미사용 시 "참여 부족"으로 리셋 위험 → 실제로 반복 사용할 테스터 확보가 핵심.

## 5. 이후
- 클로즈드 통과 → 프로덕션 전환 신청 → Google 검토(보통 7일 이내) → 단계적 출시 권장.

---
값이 바뀌면 `docs/release/store-listing.md`(문구)·이 파일을 함께 갱신.
