# 지금 상태와 다음 할 일

> 이 파일은 **PC 사이를 넘어가는 유일한 인수인계 수단**이다.
> Claude의 로컬 메모리는 PC를 넘어오지 않는다. 작업을 마칠 때 `/handoff`가 이 파일을 갱신하고,
> 다른 PC에서는 `/resume`이 이 파일을 읽어 이어받는다.

**마지막 갱신**: 2026-08-31 · 작업 PC: 메인 데스크톱
<!-- 해시는 적지 않는다. 이 파일을 커밋하는 순간 값이 바뀌어 항상 한 칸 어긋난다.
     정확한 시점은 `git log -1 -- docs/STATE.md` 로 확인할 것. -->
**검증 상태**: `npx tsc --noEmit` 0오류 · `npx jest` 18건 · 백엔드 23건 — 전부 통과(2026-08-31 실행)

---

## 지금 상태

### 저장소
- 브랜치 `main`, origin과 동기화됨. 작업 트리 깨끗함.
- 미머지 브랜치 `worktree-store-screenshots`가 남아 있으나 **내용은 이미 main에 머지됨**(`b283b8b`). 정리해도 무방.
- **2026-08-31 PC 간 인수인계 체계 도입.** Claude 로컬 메모리가 PC를 넘지 못해
  다른 PC에서 매번 맥락을 잃던 문제를 해결했다. `CLAUDE.md`(세션마다 자동 로드) →
  `docs/STATE.md`(이 파일) 순으로 읽히고, `/handoff`로 저장·`/resume`으로 이어받는다.
  **작업을 마칠 때 `/handoff`를 쓰는 것이 이 체계의 전제다.**

### 앱
- **versionCode 5 / versionName 1.2 / targetSdk 36**
- 2026-08-30 **서버 연결 오류 수정 완료**. 증상 "해몽을 가져오지 못했습니다"의 원인은
  코드 버그가 아니라 **Render 무료 플랜의 유휴 슬립**이었다(실측 `/health` 콜드 52.5초 / 웜 0.07초).
  앱에 워밍업 핑·재시도·타임아웃·진행 표시가 전무해 세션 첫 해몽이 죽은 스피너로 1분을 끌거나
  스핀업 중 502 HTML을 받고 끝났다.
  - 신규 `frontend/src/logic/serverWarmup.ts` — 앱 기동·포그라운드 복귀 시 `/health` 핑,
    동시 호출 공유, 10분 TTL, 실패해도 무해
  - `fetchReading` — 재시도·지수 백오프·타임아웃, `ReadingError.kind` 분류
    (4xx는 재시도 안 함, 200-non-JSON도 서버 오류로 취급)
  - `ResultScreen` — 경과 시간별 로더 문구, 실패 시 [다시 시도] 버튼
  - 단위 테스트 18건 신규, tsc 0 / eslint 0 / 백엔드 23건 통과
  - 에뮬(API 37) 실기 검증: Render를 39분 재운 뒤 앱 실행 → 온보딩·입력 후 제출까지 169초 →
    **해몽 12초 만에 성공**. 비행기 모드 제출 → 오류 화면 → 복구 후 [다시 시도] → 성공.

### Play Console (저장소만 봐서는 알 수 없는 정보)
- 앱 `com.mswon.monkey` / 앱ID `4973430615478884995` / 트랙 `비공개 테스트 - Alpha` `4699303582453749717`
- **테스터 34명 등록**(`나` 1 + `테스터` 33), 링크 배포 완료
- **2026-08-31 제출 완료, 둘 다 검토 중:**
  - AAB versionCode 5 (`1.2 (5) - 서버 연결 오류 수정`)
  - 스토어 스크린샷 5장 교체 (홈 → 카드 → 해몽 → 꿈기록 → 통계)
- 테스터가 **현재 쓰는 빌드는 versionCode 1**(2026-08-24 게시, targetSdk 35, 7월 빌드).
  심사 통과 시 자동 업데이트로 대체된다.
- 검토는 보통 7일 이내.

### 백엔드
- Render 무료 플랜 유지 중. `https://monkey-backend-htu8.onrender.com`
- `render.yaml`엔 `plan: starter`가 적혀 있으나 **대시보드 미적용** — 실서비스는 무료다.

---

## 다음에 할 일

1. **Play 심사 결과 확인** (수동, 며칠 뒤)
   통과하면 테스터에게 versionCode 5가 내려가고 서버 연결 오류가 해소된다.
   거부되면 사유를 보고 대응.

2. **Render 무료 플랜 지켜보기** — 사용자 결정으로 일단 무료 유지.
   워밍업은 콜드스타트를 *없앤 게 아니라 사용자 입력 시간 뒤로 숨긴 것*이다.
   앱을 켜자마자 바로 제출하는 사용자는 여전히 최대 1분 기다린다.
   테스터가 "첫 실행이 느리다"고 하면 그때 starter($7/월) 적용.

3. **테스터 12명 × 14일 연속 옵트인 유지** — 프로덕션 전환 요건. 인원(34명)은 충족.

4. **AAB 크기 130MB 문제 (미착수)**
   그중 **94MB가 카드 아트 30장**(장당 약 3MB). 테스터 다운로드 부담이 크다.
   표시 해상도로 리사이즈·최적화하면 크게 줄일 수 있다. R8은 도움이 안 된다(코드가 아니라 이미지가 원인).

5. **알려진 미해결**: `MainActivity`에 `configChanges` 없음 → 화면 회전 시 상태 유실.

---

## 최근에 알게 된 것 (같은 삽질 반복 금지)

- **Play 업로드 거부의 원인은 서명 키였다.** "파일 크기 10MB 한도 때문"이라는 이전 기록은 틀렸다 —
  그건 Claude 업로드 도구의 제약일 뿐 Play와 무관하다.
  `keystore.properties`가 없는 PC에서 빌드하면 Gradle이 조용히 디버그 키로 폴백한다.
  **업로드 전 지문 대조 필수** → `docs/release/play-console-checklist.md` 2-2절
- **Play Console 스토어 등록정보 페이지엔 `input[type=file]`이 DOM에 없다**(AAB 페이지와 다름).
  자동 업로드 방법과 썸네일 재정렬(HTML5 드래그) 방법은 `todo.md`의 스크린샷 항목에 기록해 뒀다.
- 스크린샷 1080×2400(9:20)은 안내문(16:9/9:16)과 달라도 Play가 그대로 수용했다.
- Chrome 자동화 시 **창이 포그라운드가 아니면 스크린샷이 타임아웃**된다.
